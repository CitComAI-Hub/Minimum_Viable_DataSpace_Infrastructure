# Local Dataspace Apps

Esta carpeta queda reservada para los componentes adicionales del caso de uso. El Trust Anchor se despliega desde el `main.tf` principal después de que el `Makefile` haya creado el clúster mediante `kind_cluster`.